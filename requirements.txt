pandas>=1.5
numpy>=1.23
scikit-learn>=1.2
imbalanced-learn>=0.11
xgboost>=1.7
shap>=0.42
matplotlib>=3.6
seaborn>=0.12
streamlit>=1.28
joblib>=1.2
networkx>=3.0
scipy>=1.9

# New for v2 — install these separately:
# pip install tensorflow          (for Autoencoder)
# pip install torch               (for GNN)
# pip install torch-geometric     (for GNN - see instructions below)
# pip install pyvis               (for interactive graph - optional)

# PyTorch Geometric install (CPU):
# pip install torch-geometric
# pip install pyg-lib torch-scatter torch-sparse torch-cluster torch-spline-conv \
#             -f https://data.pyg.org/whl/torch-2.1.0+cpu.html
